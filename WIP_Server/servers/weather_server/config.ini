[server]
# サーバー自身の設定
host = 0.0.0.0
port = ${WEATHER_SERVER_PORT}
debug = ${WIP_DEBUG}
max_workers = 10

[connections]
# 接続先サーバーの設定
location_server_host = ${LOCATION_RESOLVER_HOST}
location_server_port = ${LOCATION_RESOLVER_PORT}
query_server_host = ${QUERY_GENERATOR_HOST}
query_server_port = ${QUERY_GENERATOR_PORT}

[network]
# ネットワーク設定
udp_buffer_size = ${UDP_BUFFER_SIZE}

[system]
# システム設定
protocol_version = ${PROTOCOL_VERSION}

[cache]
# キャッシュの有効期限（秒）
expiration_time_weather = 1800  # デフォルト30分
expiration_time_area = 604800  # デフォルト1週間

[auth]
# 認証機能設定（プロキシサーバー用）
enable_auth = ${WEATHER_SERVER_AUTH_ENABLED}
passphrase = ${WEATHER_SERVER_PASSPHRASE}
# 接続先サーバーの認証設定
location_server_auth_enabled = ${LOCATION_RESOLVER_AUTH_ENABLED}
location_server_passphrase = ${LOCATION_RESOLVER_PASSPHRASE}
query_server_auth_enabled = ${QUERY_GENERATOR_AUTH_ENABLED}
query_server_passphrase = ${QUERY_GENERATOR_PASSPHRASE}
report_server_auth_enabled = ${REPORT_SERVER_AUTH_ENABLED}
report_server_passphrase = ${REPORT_SERVER_PASSPHRASE}
